apiVersion: v1
kind: Service
metadata:
  name: minecraft
  namespace: minecraft
spec:
  clusterIP: None
  selector:
    app: minecraft
  ports:
    - name: minecraft
      port: 25565
      targetPort: 25565
      protocol: TCP
