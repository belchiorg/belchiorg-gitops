apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-data
  namespace: anytype
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 20Gi
