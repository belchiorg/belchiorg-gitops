apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: node-storage
  namespace: anytype
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
